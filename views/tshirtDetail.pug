extends layout 

block content
    div.detail-container 
        span.detail-card 
            img(src= tshirt.imageURL).detail-item-img
            div.detail-info
                p.detail-name #{tshirt.name}
                    a(href= tshirt.url + "/edit").edit-link
                        img(src="/images/pencil.svg", alt="").edit-img
                    a(href= tshirt.url + "/delete").delete-link 
                        img(src="/images/delete.svg", alt="").delete-img
                p.detail-tax MRP inclusive of all taxes
                p.detail-price= tshirt.price
                .size-selector 
                    each val in tshirt.sizes 
                        button(onclick=`selectSize('${val}')`).size-button=  val
                button.add-to-bag Add to Bag
                div.product-description-fit
                    div.product-description-fit-title Description & fit
                    div.product-description= tshirt.description
                    div.product-fit
                    each val in tshirt.fit
                        p.fit= val
    
    