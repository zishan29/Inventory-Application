extends layout 

block content
    form(method='POST' action='').delete-form
        label= `Do you really want to delete ${shirt.name}?`
        div.form-group
            input#id.form-control(type='hidden',name='id', required='true', value=shirt._id )
        button.btn.btn-primary(type='submit') Delete