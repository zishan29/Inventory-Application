extends layout

block content
  div.container 
    each val in jeans
      a(href= val.url) 
        span.card 
            img(src= val.imageURL, loading="lazy").item-img
            p.name #{val.name}
            p.price= val.price
    each val in shirts 
      a(href= val.url) 
        span.card 
            img(src= val.imageURL, loading="lazy").item-img
            p.name #{val.name}
            p.price= val.price
    each val in shoes
      a(href= val.url) 
        span.card 
            img(src= val.imageURL, loading="lazy").item-img
            p.name #{val.name}
            p.price= val.price
    each val in sweatshirts 
      a(href= val.url) 
        span.card 
            img(src= val.imageURL, loading="lazy").item-img
            p.name #{val.name}
            p.price= val.price
    each val in trousers 
      a(href= val.url) 
        span.card 
            img(src= val.imageURL, loading="lazy").item-img
            p.name #{val.name}
            p.price= val.price
    each val in tshirts
      a(href= val.url) 
        span.card 
            img(src= val.imageURL, loading="lazy").item-img
            p.name #{val.name}
            p.price= val.price
